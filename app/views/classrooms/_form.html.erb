<%= form_for [@school, @classroom] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :code %>
  <%= f.text_field :code %>

  <%= f.label :active %>
  <%= f.select :active, [true, false], {}, class: 'browser-default' %>

 
  <%= f.submit nil, class: 'btn' %>
<% end %>